# CoMotion-X dependencies.
# Keep this file synchronized with pyproject.toml when dependencies change.

# Runtime
matplotlib>=3.9,<4
mujoco>=3.3,<4
numpy>=1.26,<3
python-dotenv>=1.0,<2

# Step 1 development and validation
pytest>=8.0,<9
ruff>=0.6,<1
